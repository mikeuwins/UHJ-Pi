// GUI Prototype - Pre XY Panner Version
// This version is the last known good state before the XY panner feature was introduced.
// All XY panner code and logic have been removed.
// Use this as a stable baseline for further development.

// ...existing code from GUIPrototype.scd (Pre XY) follows...

(
var win, mainView;
// ...existing code...
// --- XY Panner (removed: revert to pre-panner state) ---
// (No XY panner view, overlay, or related variables)

win.front;
)
